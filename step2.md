## **미션 배경**

서비스가 모바일 앱으로 확장된다. 사용자는 웹에서만 예약을 관리하지 않는다. 모바일 앱에서도 로그인하고, 예약을 확인하고, 필요한 작업을 수행하려고 한다.

웹에서 사용한 인증 방식이 모바일 앱 요청에도 그대로 적절한지 검토해야 한다. 브라우저와 모바일 앱은 인증 정보를 저장하고 전달하는 방식이 다를 수 있다.

2단계에서는 모바일 앱 요청에서도 로그인한 사용자를 식별할 수 있게 만든다.

## **목표**

- 모바일 앱 요청에서도 로그인한 사용자를 식별한다.
- 모바일 앱 사용자가 인증이 필요한 API를 사용할 수 있게 한다.
- 인증 정보가 없거나 유효하지 않은 요청을 거부한다.
- 웹 인증 구현과 모바일 인증 구현의 공통점과 차이점을 설명한다.
- 세션과 토큰 중 선택한 방식의 이유를 설명한다.

## **요구사항**

### **모바일 로그인**

- 모바일 앱 사용자는 로그인할 수 있다.
- 로그인 성공 후 모바일 앱이 이후 요청에서 사용할 인증 정보를 받을 수 있어야 한다.
- 인증 정보는 이후 요청마다 서버가 사용자를 식별할 수 있는 형태여야 한다.

### **모바일 인증 요청**

- 모바일 앱은 인증이 필요한 API를 호출할 때 인증 정보를 함께 전달한다.
- 서버는 전달된 인증 정보를 검증한다.
- 인증 정보가 유효하면 로그인한 사용자로 요청을 처리한다.
- 인증 정보가 없거나 유효하지 않으면 요청을 거부한다.

### **웹 인증과의 관계**

- 웹 인증 흐름과 모바일 인증 흐름이 어떤 점에서 같은지 설명할 수 있어야 한다.
- 웹 인증 흐름과 모바일 인증 흐름이 어떤 점에서 다른지 설명할 수 있어야 한다.
- 가능한 한 중복된 인증 로직을 줄인다.

## **구현 조건**

- 세션과 토큰 중 하나를 선택하거나, 두 방식을 조합할 수 있다.
- 선택한 방식 외의 다른 후보도 검토한다.
- 인증 정보를 전달하는 위치를 명확히 정한다.
- 컨트롤러는 웹인지 모바일인지에 따라 인증 로직이 과도하게 갈라지지 않도록 한다.

## **구현을 하며 선택해야하는 사항**

2단계의 핵심 선택은 “웹에서 만든 인증 흐름을 모바일 앱 요청에 그대로 확장할 것인가, 모바일 앱에 맞게 다른 방식을 선택할 것인가”다.

교육생은 다음을 직접 선택한다.

- 모바일 앱 요청의 인증 방식
- 예: 세션을 유지할 것인가, 토큰을 발급할 것인가, 웹과 모바일을 다르게 처리할 것인가.
- 인증 정보 전달 위치
- 예: 쿠키로 전달할 것인가, Authorization 헤더로 전달할 것인가, 다른 헤더를 사용할 것인가.
- 웹 인증 흐름과 모바일 인증 흐름의 통합 방식
- 예: 같은 검증 로직을 재사용할 것인가, 클라이언트별 어댑터를 둘 것인가.
- 만료와 로그아웃 처리 방식
- 예: 서버에서 상태를 제거할 것인가, 토큰 만료 시간에 맡길 것인가, 별도 무효화 저장소를 둘 것인가.
- 인증 실패 응답 방식
- 예: 웹과 모바일에 같은 에러 응답을 줄 것인가, 클라이언트별 응답을 나눌 것인가.

공유회에서는 “세션이냐 토큰이냐”만 비교하지 않는다. 선택한 방식이 웹과 모바일 요청을 얼마나 일관되게 설명하는지, 로그아웃과 만료 같은 운영 상황에서 어떤 불편함이 생기는지 비교한다.

## **생각해 볼 점**

- 브라우저와 모바일 앱은 인증 정보를 어떻게 다르게 다루는가?
- 세션을 모바일 앱에서 그대로 사용하면 어떤 점이 편하고 어떤 점이 불편한가?
- 토큰을 사용하면 어떤 점이 편하고 어떤 점이 불편한가?
- 로그아웃과 만료는 어떻게 처리할 것인가?
- 서버가 상태를 기억하는 방식과 기억하지 않는 방식은 어떤 차이가 있는가?

## **제출**

2단계만 별도 PR로 제출하지 않는다. 1단계, 2단계, 3단계 요구사항을 모두 구현한 뒤 하나의 PR로 제출한다.

최종 PR 설명에는 2단계와 관련해 다음 양식을 포함한다.

```
선택 도구:다른 후보:선택 이유:불편하거나 아쉬운 점:
```

## **완료 기준**

- 모바일 앱 요청에서도 로그인한 사용자를 식별할 수 있다.
- 인증 정보가 없거나 유효하지 않은 모바일 요청을 거부한다.
- 선택한 인증 방식과 다른 후보를 비교해 설명했다.
- 웹 인증 흐름과 모바일 인증 흐름의 공통점과 차이점을 설명했다.